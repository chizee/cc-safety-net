{
  "name": "cc-safety-net",
  "version": "1.0.6",
  "description": "A coding agent CLI hook - block destructive git and filesystem commands before execution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "bin": {
    "cc-safety-net": "dist/bin/cc-safety-net.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run clean && bun run scripts/build.ts",
    "build:types": "tsc --project tsconfig.build.json --emitDeclarationOnly --declaration",
    "build:schema": "bun run scripts/build-schema.ts",
    "verify:build": "bun run scripts/verify-deterministic-build.ts",
    "verify:package": "bun run scripts/verify-package.ts",
    "verify:repository-plugin": "bun run scripts/verify-repository-plugin.ts",
    "verify:coverage": "bun run scripts/verify-coverage.ts",
    "clean": "rm -rf dist",
    "check": "bun run lint && bun run typecheck && bun run knip && bun run check-duplicates && bun run sg:scan && AGENT=1 bun test tests --coverage --coverage-reporter=lcov && bun run verify:coverage",
    "check:ci": "bun run lint:ci && bun run typecheck && bun run knip && bun run check-duplicates && bun run sg:scan && AGENT=1 bun test tests --coverage --coverage-reporter=lcov && bun run verify:coverage",
    "lint": "biome check --write",
    "lint:ci": "biome ci .",
    "typecheck": "tsc --noEmit",
    "knip": "knip --production",
    "check-duplicates": "bunx jscpd src tests --exitCode 1 --reporters ai",
    "sg:scan": "ast-grep scan",
    "test": "bun test",
    "cc-safety-net": "bun run src/bin/cc-safety-net.ts",
    "prepare": "husky && bun run setup-hooks",
    "setup-hooks": "chmod +x .husky/pre-commit .husky/pre-push"
  },
  "author": {
    "name": "J Liew",
    "email": "jliew@420024lab.com"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kenryu42/cc-safety-net.git"
  },
  "bugs": {
    "url": "https://github.com/kenryu42/cc-safety-net/issues"
  },
  "homepage": "https://github.com/kenryu42/cc-safety-net#readme",
  "devDependencies": {
    "@ast-grep/cli": "^0.40.4",
    "@biomejs/biome": "2.3.10",
    "@opencode-ai/plugin": "^1.0.224",
    "@types/bun": "latest",
    "@types/shell-quote": "^1.7.5",
    "husky": "^9.1.7",
    "jscpd": "^4.0.9",
    "knip": "^5.79.0",
    "lint-staged": "^16.2.7",
    "shell-quote": "^1.8.4"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.0.224"
  },
  "peerDependenciesMeta": {
    "@opencode-ai/plugin": {
      "optional": true
    }
  },
  "pi": {
    "extensions": [
      "./dist/pi/index.js"
    ]
  },
  "dependencies": {
    "zod": "4.3.5"
  },
  "trustedDependencies": [
    "@ast-grep/cli"
  ],
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.14",
  "keywords": [
    "claude-code",
    "opencode",
    "safety",
    "plugin",
    "security",
    "pi-package"
  ]
}
