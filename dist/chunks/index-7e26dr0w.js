import{isUnsupportedWindowsNamespacePath}from"./index-sg1ywysy.js";import{realpathSync,statSync}from"node:fs";import{isAbsolute,relative,resolve,sep}from"node:path";function resolveContainedCwd(requestedCwd,trustedRoots){if(isUnsupportedWindowsNamespacePath(requestedCwd))return;let roots=trustedRoots.flatMap((root)=>canonicalDirectory(root));if(!roots[0])return;let requested=canonicalDirectory(isAbsolute(requestedCwd)?requestedCwd:resolve(roots[0],requestedCwd))[0];if(!requested)return;return roots.some((root)=>isSameOrInsidePath(requested,root))?requested:void 0}function firstTrustedRoot(trustedRoots){return trustedRoots.flatMap((root)=>canonicalDirectory(root))[0]}function canonicalDirectory(path){try{let realPath=realpathSync(path);return statSync(realPath).isDirectory()?[realPath]:[]}catch{return[]}}function isSameOrInsidePath(path,root){let rel=relative(root,path);return rel===""||rel!==".."&&!rel.startsWith(`..${sep}`)&&!isAbsolute(rel)}
export{resolveContainedCwd,firstTrustedRoot,isSameOrInsidePath};
